#pragma once
#include <string>
class GPU
{
public:
    GPU(); // Constructor
    std::string completarContenidoReg(const std::string& regBase); // Completa el contenido del archivo .reg con las claves correspondientes a la marca de la GPU.
};

