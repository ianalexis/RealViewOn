# Workflow para actualizar herramientas de compresión y empaquetado

name: Update Compression Tools

on:
  workflow_dispatch:

env:
  TOOLS_DIR: tools

permissions:
  contents: write

jobs:
  update-tools:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get current and latest versions
        id: versions
        run: |
          Write-Host "🔍 Verificando versiones de herramientas..."
          
          # Headers comunes para GitHub API
          $headers = @{
            'Accept' = 'application/vnd.github.v3+json'
            'User-Agent' = 'RealViewOn-UpdateBot'
          }
          
          # Función para obtener versión actual
          function Get-CurrentVersion {
            param($ToolPath, $VersionPattern, $ToolName)
            
            if (Test-Path $ToolPath) {
              try {
                $cmd = if ($ToolName -eq "7zr") { & $ToolPath 2>&1 } else { & $ToolPath --version 2>&1 }
                $output = $cmd | Select-String $VersionPattern | Select-Object -First 1
                if ($output -and $output -match $VersionPattern) {
                  return $matches[1]
                }
              } catch {
                Write-Host "⚠️ Error al obtener versión de $ToolName : $($_.Exception.Message)"
              }
            }
            return "No encontrado"
          }
          
          # Función para obtener última versión de GitHub
          function Get-LatestRelease {
            param($Repo, $AssetPattern, $ToolName)
            
            try {
              $release = Invoke-RestMethod -Uri "https://api.github.com/repos/$Repo/releases/latest" -Headers $headers
              if (-not $release) { return $null }
              
              $version = $release.tag_name -replace '^v', ''
              $asset = $release.assets | Where-Object { $_.name -match $AssetPattern } | Select-Object -First 1
              
              if ($asset) {
                Write-Host "✅ $ToolName v$version encontrado: $($asset.name)"
                return @{
                  Version = $version
                  DownloadUrl = $asset.browser_download_url
                  Found = $true
                }
              } else {
                Write-Host "❌ No se encontró asset compatible para $ToolName"
                return @{ Found = $false }
              }
            } catch {
              Write-Host "❌ Error al obtener $ToolName : $($_.Exception.Message)"
              return @{ Found = $false }
            }
          }
          
          # Obtener versiones actuales
          $current7zr = Get-CurrentVersion "${{ env.TOOLS_DIR }}\7zr.exe" "7-Zip.*?(\d+\.\d+)" "7zr"
          $currentUpx = Get-CurrentVersion "${{ env.TOOLS_DIR }}\upx.exe" "upx\s+(\d+\.\d+\.\d+)" "upx"
          
          # Obtener últimas versiones
          $latest7zr = Get-LatestRelease "ip7z/7zip" "(7zr.*\.exe)$" "7-Zip"
          $latestUpx = Get-LatestRelease "upx/upx" "upx-.*-win64\.zip$" "UPX"
          
          Write-Host "📋 Versiones actuales vs disponibles:"
          Write-Host "   7zr.exe: $current7zr vs $($latest7zr.Version)"
          Write-Host "   upx.exe: $currentUpx vs $($latestUpx.Version)"
          
          # Determinar actualizaciones necesarias
          $need7zrUpdate = $latest7zr.Found -and ($current7zr -eq "No encontrado" -or $current7zr -ne $latest7zr.Version)
          $needUpxUpdate = $latestUpx.Found -and ($currentUpx -eq "No encontrado" -or $currentUpx -ne $latestUpx.Version)
          $needAnyUpdate = $need7zrUpdate -or $needUpxUpdate
          
          Write-Host "📋 Actualizaciones necesarias: $(if($needAnyUpdate) { 'SÍ' } else { 'NO' })"
          if ($need7zrUpdate) { Write-Host "   🔄 7zr.exe: $current7zr → $($latest7zr.Version)" }
          if ($needUpxUpdate) { Write-Host "   🔄 upx.exe: $currentUpx → $($latestUpx.Version)" }
          
          # Guardar outputs
          echo "CURRENT_7ZR_VERSION=$current7zr" >> $env:GITHUB_OUTPUT
          echo "CURRENT_UPX_VERSION=$currentUpx" >> $env:GITHUB_OUTPUT
          echo "LATEST_7ZR_VERSION=$($latest7zr.Version)" >> $env:GITHUB_OUTPUT
          echo "LATEST_UPX_VERSION=$($latestUpx.Version)" >> $env:GITHUB_OUTPUT
          echo "DOWNLOAD_URL_7ZR=$($latest7zr.DownloadUrl)" >> $env:GITHUB_OUTPUT
          echo "DOWNLOAD_URL_UPX=$($latestUpx.DownloadUrl)" >> $env:GITHUB_OUTPUT
          echo "NEED_7ZR_UPDATE=$need7zrUpdate" >> $env:GITHUB_OUTPUT
          echo "NEED_UPX_UPDATE=$needUpxUpdate" >> $env:GITHUB_OUTPUT
          echo "NEED_ANY_UPDATE=$needAnyUpdate" >> $env:GITHUB_OUTPUT

      - name: Create tools directory
        if: steps.versions.outputs.NEED_ANY_UPDATE == 'True'
        run: |
          New-Item -ItemType Directory -Force -Path "${{ env.TOOLS_DIR }}" | Out-Null
          Write-Host "📁 Directorio ${{ env.TOOLS_DIR }} listo"

      - name: Update tools
        if: steps.versions.outputs.NEED_ANY_UPDATE == 'True'
        run: |
          # Función para descargar y actualizar herramienta
          function Update-Tool {
            param($Name, $DownloadUrl, $TargetPath, $IsZip = $false)
            
            $tempFile = "temp_$Name" + $(if($IsZip) { ".zip" } else { ".exe" })
            
            try {
              Write-Host "📥 Descargando $Name desde: $DownloadUrl"
              
              # Configurar timeout y parámetros de descarga
              $progressPreference = 'SilentlyContinue'  # Desactivar barra de progreso que puede causar problemas
              
              # Usar Invoke-WebRequest con timeout configurado correctamente
              Write-Host "   ⏳ Iniciando descarga..."
              $downloadParams = @{
                Uri = $DownloadUrl
                OutFile = $tempFile
                UserAgent = "RealViewOn-UpdateBot"
                TimeoutSec = 300  # 5 minutos timeout
                UseBasicParsing = $true  # Evita problemas con IE parser
              }
              
              Invoke-WebRequest @downloadParams
              
              # Verificar que el archivo se descargó correctamente
              if (-not (Test-Path $tempFile) -or (Get-Item $tempFile).Length -eq 0) {
                throw "El archivo descargado está vacío o no existe"
              }
              
              $fileSize = (Get-Item $tempFile).Length / 1MB
              Write-Host "   ✅ Descarga completada ($([math]::Round($fileSize, 2)) MB)"
              
              if ($IsZip) {
                # Extraer ZIP y buscar específicamente upx.exe
                Write-Host "   📦 Extrayendo archivo ZIP..."
                $extractDir = "temp_${Name}_extract"
                Expand-Archive -Path $tempFile -DestinationPath $extractDir -Force
                
                # Buscar upx.exe recursivamente en todos los subdirectorios
                $exeFile = Get-ChildItem -Path $extractDir -Name "upx.exe" -Recurse | Select-Object -First 1
                
                if ($exeFile) {
                  $fullPath = Get-ChildItem -Path $extractDir -Filter "upx.exe" -Recurse | Select-Object -First 1 | Select-Object -ExpandProperty FullName
                  Write-Host "   📋 Copiando $fullPath a $TargetPath"
                  Copy-Item $fullPath $TargetPath -Force
                  Remove-Item -Recurse -Force $extractDir -ErrorAction SilentlyContinue
                } else {
                  # Listar contenido del ZIP para debug
                  $content = Get-ChildItem -Path $extractDir -Recurse | Select-Object Name, FullName
                  Write-Host "   ⚠️ Contenido del ZIP:"
                  $content | ForEach-Object { Write-Host "      $($_.Name) - $($_.FullName)" }
                  throw "upx.exe no encontrado en el ZIP $tempFile"
                }
              } else {
                # Ejecutable directo (para 7zr.exe)
                Write-Host "   📋 Copiando ejecutable a $TargetPath"
                Copy-Item $tempFile $TargetPath -Force
              }
              
              # Verificar funcionamiento
              Write-Host "   🔍 Verificando funcionamiento..."
              $version = if ($Name -eq "7zr") {
                & $TargetPath 2>&1 | Select-String "7-Zip" | Select-Object -First 1
              } else {
                & $TargetPath --version 2>&1 | Select-String "upx" | Select-Object -First 1
              }
              Write-Host "✅ $Name actualizado: $version"
              
              Remove-Item $tempFile -Force -ErrorAction SilentlyContinue
            } catch {
              Write-Host "❌ Error al actualizar $Name : $($_.Exception.Message)"
              Write-Host "   🔍 Detalles del error: $($_.Exception.GetType().Name)"
              
              # Limpiar archivos temporales
              Remove-Item $tempFile -Force -ErrorAction SilentlyContinue
              Remove-Item "temp_${Name}_extract" -Recurse -Force -ErrorAction SilentlyContinue
              
              # Re-lanzar el error para que el workflow falle
              throw "Fallo al actualizar $Name"
            }
          }
          
          # Actualizar herramientas según sea necesario
          if ("${{ steps.versions.outputs.NEED_7ZR_UPDATE }}" -eq "True") {
            Update-Tool "7zr" "${{ steps.versions.outputs.DOWNLOAD_URL_7ZR }}" "${{ env.TOOLS_DIR }}\7zr.exe"
          }
          
          if ("${{ steps.versions.outputs.NEED_UPX_UPDATE }}" -eq "True") {
            Update-Tool "upx" "${{ steps.versions.outputs.DOWNLOAD_URL_UPX }}" "${{ env.TOOLS_DIR }}\upx.exe" $true
          }

      - name: Commit changes
        if: steps.versions.outputs.NEED_ANY_UPDATE == 'True'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "${{ env.TOOLS_DIR }}/"
          
          $changes = @()
          if ("${{ steps.versions.outputs.NEED_7ZR_UPDATE }}" -eq "True") {
            $changes += "7zr.exe: ${{ steps.versions.outputs.CURRENT_7ZR_VERSION }} → ${{ steps.versions.outputs.LATEST_7ZR_VERSION }}"
          }
          if ("${{ steps.versions.outputs.NEED_UPX_UPDATE }}" -eq "True") {
            $changes += "upx.exe: ${{ steps.versions.outputs.CURRENT_UPX_VERSION }} → ${{ steps.versions.outputs.LATEST_UPX_VERSION }}"
          }
          
          $message = "🔧 Actualizar herramientas de compresión`n`n- " + ($changes -join "`n- ") + "`n`nActualización automática desde GitHub Actions"
          
          if (git status --porcelain) {
            git commit -m $message
            git push origin main
            Write-Host "✅ Cambios enviados al repositorio"
          } else {
            Write-Host "ℹ️ No hay cambios para hacer commit"
          }

      - name: Summary
        if: always()
        run: |
          Write-Host "📋 RESUMEN DE ACTUALIZACIÓN"
          Write-Host "=" * 40
          
          if ("${{ steps.versions.outputs.NEED_ANY_UPDATE }}" -eq "True") {
            Write-Host "🎉 Actualizaciones realizadas:"
            if ("${{ steps.versions.outputs.NEED_7ZR_UPDATE }}" -eq "True") {
              Write-Host "   ✅ 7zr.exe: ${{ steps.versions.outputs.CURRENT_7ZR_VERSION }} → ${{ steps.versions.outputs.LATEST_7ZR_VERSION }}"
            }
            if ("${{ steps.versions.outputs.NEED_UPX_UPDATE }}" -eq "True") {
              Write-Host "   ✅ upx.exe: ${{ steps.versions.outputs.CURRENT_UPX_VERSION }} → ${{ steps.versions.outputs.LATEST_UPX_VERSION }}"
            }
          } else {
            Write-Host "ℹ️ Herramientas ya actualizadas:"
            Write-Host "   📋 7zr.exe: ${{ steps.versions.outputs.CURRENT_7ZR_VERSION }}"
            Write-Host "   📋 upx.exe: ${{ steps.versions.outputs.CURRENT_UPX_VERSION }}"
          }
          
          Write-Host ""
          Write-Host "🔗 Releases: https://github.com/ip7z/7zip/releases | https://github.com/upx/upx/releases"
